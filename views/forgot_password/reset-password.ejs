<!-- C:\Users\chanya\Desktop\TaskManagement\views\login_register\reset_password.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="../css/reset_password.css">
</head>
<body>
    <div class="reset-wrapper">
        <div class="header-wrapper">
            <!-- Back to login icon and link -->
            <a href="/forgot-password" class="tooltip">
              <i class='bx bxs-chevron-left-circle bx-flip-vertical' style='color:#7952b3; font-size: 30px;'  ></i><span class="tooltiptext">กลับไปหน้าลืมรหัสผ่าน</span>
            </a>
            <h1 class="reset-title">รีเซ็ตรหัสผ่าน</h1>
          </div>
        <form action="/reset-password" method="POST">
            <% if (error && error.length > 0) { %>
                <div class="reset-alert"><%= error.join(', ') %></div>
            <% } %>
            <% if (success && success.length > 0) { %>
                <div class="reset-alert success"><%= success.join(', ') %></div>
            <% } %>
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="reset-input-box">
                <span class="reset-span-text">รหัสผ่านใหม่:</span>
                <input type="password" name="newPassword" placeholder="รหัสผ่านใหม่" required minlength="8">
            </div>
            <button type="submit" class="reset-btn">รีเซ็ตรหัสผ่าน</button>
        </form>
    </div>
</body>
</html>
